# 技术方案重写实施计划

1. 备份式读取原文结构，保留有效原则、技术选型和示例事实。
2. 按批准后的 15 章结构重写原 Markdown，合并重复内容。
3. 补齐资产模型、证据模型、候选用例模型、生命周期与状态转换规则。
4. 补齐生成隔离、覆盖保护、错误映射、LLM 接入约束和数据保留策略。
5. 对照当前仓库标注现有能力与待建设能力。
6. 校验标题层级、代码块、术语、状态数量、默认验收命令和占位符。
