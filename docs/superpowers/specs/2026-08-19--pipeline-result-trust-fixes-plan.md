# 流水线结果可信度修复实施计划

1. 在 `pipeline.py` 中统一结果状态格式化，修正报告汇总，并保留异常门禁的阻断属性。
2. 在 `gates/e2e_regression.py` 中于 npm 执行前删除固定 JSON 报告，确保只解析本次输出。
3. 新增隔离的 Python 单元测试，覆盖状态、汇总、阻断异常和旧报告清理。
4. 执行单元测试、Python 编译检查及流水线 dry-run，复核生成报告。
